<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="code.fortomorrow.foodie">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodie">


        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".Activities.AdminCategoryActivity" />
        <activity android:name=".Activities.DigitalPaymentActivity" />
        <activity android:name=".Activities.PaymentActivity" />
        <activity android:name=".Activities.KacchiVaiActivity" />
        <activity android:name=".Activities.KFCActivity" />
        <activity android:name=".Activities.ConfirmOrderActivity" />
        <activity android:name=".Activities.AllFoodActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>